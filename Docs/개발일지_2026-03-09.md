# 📅 개발일지

---

## 2026-03-09 (월요일)

### 오늘 한 일

- **Currency: 골드 시스템 및 GlobalSaveCoordinator (402ef11)**  
  CurrencyManager 추가. SaveData에 gold 필드. GlobalSaveCoordinator, CurrencySaveContributor로 Play/Global 2중 세이브 구조. GameManager 하위에 등록.

- **EnemyRewardController: 적 처치 보상 (d43e469)**  
  EnemyData에 DropEntry[], goldDrop. EnemyRewardController에서 골드 즉시 지급 + 확률 아이템 월드 생성. PlaySceneEventHub OnEnemyKilled(Enemy). QuestController ApplyQuestRewards.

- **Pool: PoolManager, Pool, Poolable (d563c5d)**  
  프리팹 참조 기반 풀링 시스템. Poolable 컴포넌트. EnemyRewardController PoolManager 연동(선택).

- **Resource: IResourceLoader, ResourceManager (8f10f43)**  
  IResourceLoader 인터페이스. ResourcesResourceLoader(Resources.Load). Addressables 전환 대비 구조.

- **UI: CurrencyView, CurrencyPresenter (507e064)**  
  골드 HUD. CurrencyPresenter가 CurrencyManager 구독.

- **ScriptableObjects Resources 이동 (25e81d6)**  
  0_ScriptableObjects를 Resources/0_ScriptableObjects로 이동. DataManager LoadAll 경로 조정.

- **Unity 6 업그레이드 및 Addressables 패키지 (877e4dd)**  
  ProjectVersion, Packages(Addressables 등), ProjectSettings.

- **Resources: Prefabs 복사 (9cdef58)**  
  00_Prefabs를 Resources/Prefabs로 복사. ResourceManager LoadPrefab 연동용.

- **Addressables 초기화 (5782b69)**  
  AddressableAssetsData 추가.

- **Scene: Temp 제거, New Scene 추가, Play 수정 (b6bcb77)**  
  Temp.unity 삭제, New Scene.unity 추가. Play.unity 변경.

- **Prefab meta, 스크립트, 0_ScriptableObjects.meta (7fd86ad)**  
  00_Prefabs meta 갱신. DialogueController, InputHandler, Inventory, CursorController, Quest 수정. 02_Scripts/0_ScriptableObjects.meta 삭제.

- **기타: Input, Settings, SaveData, ScriptableBuildPipeline (3e5dda8)**  
  InputActions, InputSystem. Settings(URP). SaveData. ScriptableBuildPipeline. Resources/0_ScriptableObjects.meta. New Render Texture.

- **Docs: 확장 계획 정리 (e453fe5)**  
  확장_계획_정리.md 추가.

### 기술 메모

- **GlobalSaveCoordinator**: Play 세이브(씬별)와 Global 세이브(계정 귀속) 2중 구조. SaveContributor 패턴으로 각 시스템이 세이브/로드 데이터 기여. Currency는 Global에 저장.

- **EnemyRewardController**: 적 데이터의 DropEntry[]와 goldDrop 기반. 골드는 즉시 CurrencyManager.AddGold. 아이템은 확률 계산 후 월드에 생성. ItemObject.Initialize(ItemData, count), Poolable 반환.

- **IResourceLoader**: Resources.Load와 Addressables를 추상화. ResourceManager가 IResourceLoader 사용. Data에 주소 문자열 저장, 실제 로드는 ResourceManager에 위임.

- **Resources/Prefabs**: 00_Prefabs 원본 유지, Resources/Prefabs에 복사. ResourceManager.LoadPrefab("Prefabs/Item/Apple") 등 경로 기반 로드 가능.

### 배운 점

- Awake 실행 순서는 보장되지 않음. 다른 오브젝트 초기화에 의존하는 작업은 Start에서 수행.

### 내일 할 일

- 

### 기타

- 

---

*Git 로그 기준 정리 (2026-03-09 커밋).*
