# 📅 개발일지

---

## 2026-03-17 (화요일)

### 오늘 한 일

- **Effect: Hit·DamageNumber·EffectAutoReturn (80a9ab6)**  
  - EffectManager, SoundManager 추가. GameManager 등록. PlaySceneEventHub.OnDamageDealt 구독.
  - RM Effect/Hit, UI/DamageText 로드. PoolManager 연동. EffectAutoReturn, DamageView로 파티클 종료 후 풀 반환.

- **Pool: 파괴된 오브젝트 처리 (3e78e8e)**  
  - Pool.Pop에서 파괴된 오브젝트 스킵. PoolManager.RemoveDestroyedFromAllPools 추가.
  - PlayScene.OnDisable에서 호출.

- **Enemy: RequireComponent·GetComponent 방식 (3ea823c)**  
  - Character처럼 RequireComponent + GetComponent. SerializeField 제거.
  - EnemyStateMachine, EnemyAttacker RequireComponent 추가. Animator는 자식(모델링)에서 GetComponentInChildren.

- **PickupLog: ItemPickupSlot→PickupLogEntry·PickupLogView (e345619)**  
  - ItemPickupSlot, ItemPickupLog 삭제. PickupLogEntry, PickupLogView, PlaySceneOverlayController 추가.
  - InteractionUI→InteractionView.

- **PlayScene·연동 정리 (aee0c13)**  
  - PlaySceneView, InventoryPresenter, Squad 등 OverlayController·PickupLog 연동.

### 배운 점

- **Data·Model·RM·DM 구조**  
  - Data(SO) = 단일 소스. prefabAddress(경로) 보관. Data가 커져도 괜찮음. 필요 시 Nested Serializable 클래스로 Inspector 접기/펼치기.
  - RM: 주소로 프리팹 로드. DM: SO 보관·조회. Data에 prefab 직접 참조보다 경로만 두면 번들 분리·업데이트에 유리.
  - assetId = 주민등록. 고유·불변. 세이브·조회·경로 컨벤션에 사용.

- **컨벤션**  
  - characterId(소문자) → Prefab 경로: Prefabs/Character/{characterId}. displayName(대문자) = UI용.

- **Nested Class**  
  - [Serializable] 클래스/구조체는 Inspector에서 접기/펼치기 가능. Custom Editor 없이 사용 가능.

### 내일 할 일

- [ ] **Data SO·Base 구조 적용**  
  - Data 부모 클래스 추가 (assetId). CharacterData, EnemyData 등 상속.
  - prefabAddress 컨벤션 (비어 있으면 Prefabs/{Category}/{assetId}).
  - DM·RM 역할 분리 유지.

- [ ] **무기별 히트 이펙트**  
  - 히트는 현재 공용 Hit 유지. 이후 WeaponData에 hitEffectId 등 추가해 무기별 이펙트 전환.

- [ ] **Enemy Data 주입**  
  - EnemyTeamData를 enemyPrefabs → enemyDatas(EnemyData[])로 변경. Data 중심 생성. Model.Initialize(data).

- [ ] **EffectData SO**  
  - EffectManager가 EffectType → EffectData 조회. EffectData에 prefabAddress, scale 등.

---

*Git 로그 기준 정리 (2026-03-17).*
